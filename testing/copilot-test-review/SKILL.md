---
name: copilot-test-review
description: >
  Analyze a single GH Copilot Agent Mode test generation run — review the log, assess generated
  test quality, and suggest concrete improvements. Use this skill when asked to review a Copilot
  test run, analyze Copilot agent output, assess Copilot-generated tests, or review Copilot test logs.
  Trigger phrases include "review copilot test run", "analyze copilot tests", "assess copilot output",
  "copilot test review", "review copilot agent".
---

# GH Copilot Agent Mode Test Review Skill

## How to Use

Invoke this skill by asking Copilot to review a GH Copilot Agent Mode test generation run. Provide:

1. **A folder** containing the Copilot run artifacts (log + `.cs` test files)
2. **The original source file** being tested (e.g., the `.cs` file under test)
3. **The LLM model used** (you will be asked if not found in the log)
4. **Coverage report** (`coverage.cobertura.xml`) if available

### Example prompts

```
Review the copilot test run in C:\path\to\CopilotFolder, source file is C:\path\to\MyClass.cs
```

```
Analyze the copilot agent output in C:\path\to\folder
```

```
Assess the tests from this copilot run: <folder>
```

### What you get

A single markdown report saved to a timestamped folder (`MMDDYYYY-CopilotTestReview-Run<N>`):

| Report | Description |
|--------|-------------|
| `MMDDYYYY-CopilotAgent-<Project>.md` | Full analysis: timeline, errors, test quality assessment, and suggested issues |

### What the folder should contain

| File | Description |
|------|-------------|
| Copilot log file | A log file (e.g., `Copilot log.txt`, `copilot-output.log`, or `copilot-output.txt`) — copy from VS "GitHub Copilot" output window or collected from `%TEMP%\VSGitHubCopilotLogs` |
| `.cs` test file(s) | The generated test file(s) — needed for quality assessment |
| `coverage.cobertura.xml` | Code coverage artifact (if collected) |

> **Tip:** Use the `collect-test-copilot-logs` skill to gather all Copilot artifacts (TRX, console output, coverage, and Copilot logs) into a timestamped folder before running this review.

---

## Purpose

Given a folder from a **GH Copilot Agent Mode** test generation run, produce a single detailed evaluation report covering the run timeline, compilation results, test quality assessment, and suggested improvements.

---

## Inputs

You will be provided:

- **Copilot run folder** — a folder containing the run artifacts. Look for a Copilot log file (e.g., `Copilot log.txt`, `copilot-output.log`, `copilot-output.txt`) and any `.cs` test file(s) generated by Copilot.
- **Run metadata (auto-detected)** — if the folder contains a `run-metadata.md` file (created by the `collect-test-copilot-logs` skill), read it to pre-populate the Prompt, Target, Date/Time, and Visual Studio Version fields. Still confirm with the user.
- **Coverage artifact (optional)** — a `coverage.cobertura.xml` file captured during the run. If missing, ask the user whether they can collect coverage using `dotnet test` with `--collect "XPlat Code Coverage"` and copy the resulting coverage file into the run artifacts folder.
- **Model used** — ask the user which LLM model was used for the run (e.g., GPT-4o, Claude 3.5 Sonnet, GPT-5.2-Codex, etc.). Include this in the run metadata.
- **Target source file** — the original `.cs` source file that was targeted for test generation. Ask the user for the file path. If not provided, extract the path from the log content (look for `#file:` mentions in the prompt, `SemanticSearchStrategy` entries, or `FileEditingState` references). Read this file to understand the actual API surface, branches, constructors, and logic — this is essential for accurate test quality classification, coverage gap analysis, and identifying missing test scenarios.
- **Output directory** — ask the user where to save the report. Default is the same folder as the Copilot input folder. Offer the option to pick a different folder.

---

## Output

Generate one `.md` file inside a **new subfolder** within the output directory.

### Output Folder

- **Folder name format:** `MMDDYYYY-CopilotTestReview-Run<N>`
- `MMDDYYYY` — the date the evaluation log was created (extract from log timestamps)
- `<N>` — a run number starting at 1, incremented for each additional run on the same day
- **Check for existing folders** in the output directory matching `MMDDYYYY-CopilotTestReview-Run*`. If none exist, use `Run1`. If `Run1` already exists, use `Run2`, etc.

### File Name

Extract `<ProjectName>` from the log — use the solution name, repo folder name, or target project name found in the log. Do not hardcode any specific project or file name.

**Format:** `MMDDYYYY-CopilotAgent-<ProjectName>.md`

**Example:** `02062026-CopilotTestReview-Run1/02062026-CopilotAgent-eShop.md`

---

## Report Sections

### Header
```
# GH Copilot Agent Mode Review — <ProjectName> (<MM/DD/YYYY>)
```

### Context
- Why was this file recommended for test generation? (extract from any available context or note as "N/A")

### Run Metadata
- **Tool:** GH Copilot Agent Mode
- **Prompt:** the exact prompt passed to Copilot (extract from log, e.g., "Generate unit tests for MyClass.cs")
- **Target:** full path to the source file(s) or folder under test
- **Date/Time:** date and time of the run (extract from log timestamps if available)
- **Visual Studio Version:** (ask the user if not present in the log, e.g., `17.14 Preview 3`)
- **Model:** the LLM model used (ask the user if not present in the log)
- **Copilot Instructions:** check whether a custom instructions file was detected or used during the run. Look for any of these in the target repo or log: `.github/copilot-instructions.md`, `.github/instructions/*.instructions.md`, `.copilot/` folder with custom instructions, or references to custom instructions in the log. Note the file path(s) if found, or "None detected" if not.
- **Log file:** full path
- **Total duration:** extract from timestamps if available; note "Unknown (log lacks per-line timestamps)" if not
- **LLM usage:** extract from the log:
  - **LLM round-trips:** count of `EventType(9)` entries (each is a request to the LLM)
  - **Total tokens:** extract from the final `InputTokenCount` / `OutputTokenCount` / `TotalTokenCount` entry
  - **Cached tokens:** extract `prompt_tokens_details_cached_tokens` if present
  - If token data is not available in the log, note "Token usage data not available in log"

### Result
- One-line summary (e.g., "15 tests generated — all passing" or "8 tests generated — unvalidated, no build/test execution performed")

### Code Coverage
- **Initial coverage:** percentage before test generation (if available; note "N/A" if Copilot does not collect coverage)
- **Final coverage:** percentage after test generation (if available; note "N/A — no build/test validation performed" if applicable)
- **Coverage change:** difference (e.g., `+15.2pp` or "Unknown — tests not validated")

### Coverage Artifact
- **Coverage file:** full path to `coverage.cobertura.xml` if provided, otherwise note "Not provided"
- **Notes:** mention whether the file was sourced from a separate test run or collected alongside the Copilot run

### Timeline
- Use a table with `| Step | Event |` columns (use step numbers instead of timestamps if log has no per-line timestamps)
- Extract key events:
  - Session initialization
  - Solution loaded
  - MCP server discovery and status
  - Auth status
  - Message sent (prompt text)
  - LSP activation / errors
  - Semantic search rounds (list search terms and result counts)
  - File edits generated (list each file and what was edited)
  - File lock warnings
  - Plan update errors
  - Follow-up user messages
  - Build/test execution (if performed)
  - Memory nudges

### Errors & Warnings
- Table with `| Severity | Description |` columns
- Include all errors, warnings from the log
- If no errors or warnings occurred, note "No errors or warnings" and skip the table

### Deleted Tests
During editing cycles, Copilot may revise, replace, or remove test methods it previously generated. Track these by comparing earlier file edit states (visible in the log via `FileEditingState` entries) against the final test file.

- **Table format:**
  ```
  | Deleted Test Method | Reason (from log) | Could It Have Been Retained? |
  ```
  - List each test method that appeared in an earlier edit but is absent from the final `.cs` file
  - Extract the reason from log entries (e.g., "Replaced during follow-up edit", "Removed after user feedback")
  - Assess whether the test could have been retained (e.g., "Yes — tested a valid edge case" or "No — was a duplicate of another test")
- If no tests were deleted or replaced, note "No tests were deleted during editing cycles" and skip the table
- **Important:** Deleted tests that tested meaningful behavior are high-value signals — flag these for the Suggested Issues section.

### Analysis
- Bullet points covering:
  - Whether build/test validation occurred
  - Scope of semantic search
  - Files edited and what they likely addressed
  - Internal plan tracking issues
  - Whether the generated code is validated or unvalidated
- If the run succeeded cleanly, summarize what went well instead

### Test Quality Assessment
If the generated test file(s) are present in the folder, read the actual test code and the target source file, then assess quality:

- **Test file(s):** list the path(s) to the generated test file(s)
- **Total test methods:** count of distinct test methods — detect the test framework in use and count accordingly:
  - **MSTest:** `[TestMethod]` methods (parameterized `[DataRow]` tests count as one method with N cases)
  - **xUnit:** `[Fact]` and `[Theory]` methods (parameterized `[InlineData]` tests count as one method with N cases)
  - **NUnit:** `[Test]` and `[TestCase]` methods (parameterized `[TestCase]` tests count as one method with N cases)
- **Test classification:** categorize each test method into one of:
  - **Behavioral** — tests meaningful logic, branching, side effects, or return values based on different inputs/states
  - **Trivial** — tests something with near-zero chance of failing (e.g., constructor creates non-null instance, constant return value called multiple times)
  - **Redundant** — duplicates the intent of another test without adding new coverage
- **Classification table:**
  ```
  | Test Method | Category | Notes |
  ```
  - List every test method with its category and a brief note on what it verifies
- **Quality summary:**
  - Count of Behavioral / Trivial / Redundant tests
  - Percentage of tests that are Behavioral
- **What's well done:** bullet points on strong testing practices observed (e.g., mock verification, proper AAA pattern, edge case coverage, testing side effects)
- **What's missing:** bullet points on gaps (e.g., exception propagation tests, concurrency, null guard tests, state transition edge cases, negative mock verification)

If no `.cs` test files are present in the folder (e.g., build failures prevented final code from being saved), note "Test quality assessment not available — no generated test file found in folder" and skip this section.

---

## Suggested Issues

After completing the analysis, suggest concrete improvements based on what was observed in this specific run. These are suggestions for the user or team to improve their Copilot-assisted test generation workflow.

Generate up to **10 issues**, each specific to patterns observed in the current run. Do not use pre-written or standing issues — every issue must be derived from evidence in this run's log, test code, or analysis findings.

### HIGH-ROI ISSUE PRIORITIZATION (Required)

When producing this section:

1) Assign an ROI tier to every issue:
   - ROI: HIGH
   - ROI: MEDIUM
   - ROI: LOW

2) Compute ROI using these criteria (use best judgment, no need to show the math):
   - Breadth (how many projects/users likely hit this): 0–3
   - Impact (time saved, correctness, reliability): 0–3
   - Fixability (clear repro + implementable heuristic): 0–2
   - Urgency (blocks runs, causes hangs/failures): 0–2
   - Total: 0–10

   Map score to tier:
   - 8–10 => ROI: HIGH
   - 5–7  => ROI: MEDIUM
   - 0–4  => ROI: LOW

3) Sort the issues by ROI tier first (HIGH, then MEDIUM, then LOW).
   Within the same tier, sort by score descending.

4) Include the ROI tier in each issue header:
   Example: `

### Issue 1 (ROI: HIGH): Tests generated but never validated via build or test execution`

5) For each issue, include a short "Why ROI" line (1 sentence) stating the main reason.

6) Do not change the substance of the issues — only add ROI labeling, sorting, and the brief "Why ROI" line.

### Issue Format

Each issue must follow this format:

```

### Issue N (ROI: <tier>): <Short title>

- **Problem:** <What was observed in this run — cite specific test methods, error codes, or log entries>

- **Suggested fix:** <Concrete, implementable change or workflow improvement>

- **Fix location:** <Where the fix would be implemented — one or more of: `LLM prompt/model`, `Copilot Agent orchestrator`, `Roslyn analyzers`, `NuGet/MSBuild tooling`, `VS test runner`, `User workflow`>

- **Why ROI:** <1 sentence — the main reason this issue merits its tier>
```

**Fix location values** — use the most specific label(s) that apply:
- **LLM prompt/model** — the fix requires better prompting, fine-tuning, or model capability (e.g., generating smarter test code)
- **Copilot Agent orchestrator** — the fix is in Copilot's agent mode control logic (e.g., tool selection, file handling, validation loops)
- **Roslyn analyzers** — the fix involves static analysis, diagnostics, or code-aware checks (e.g., detecting sealed classes before generating mocks)
- **NuGet/MSBuild tooling** — the fix involves package management, build configuration, or project file handling
- **VS test runner** — the fix involves test discovery, execution, or caching in Visual Studio
- **User workflow** — the fix is a recommendation for the user (e.g., extracting an interface, changing project structure)

**Common patterns to look for (use only if evidenced):**
- Tests generated but never built or executed (unvalidated code)
- Trivial / filler tests (e.g., constructor-not-null, constant return value re-tests)
- Missing negative mock verification (`DidNotReceive` / `Times.Never`)
- Exception propagation tests not generated
- Cancellation token not tested for actual cancellation scenarios
- Tests that duplicate intent with different setup but same assertion
- Missing state transition edge cases
- Unnecessary test setup complexity
- Missing async edge cases (e.g., `Task.FromException`, `Task.FromCanceled`)
- Not testing all branches visible in the source (if/else, switch, null checks)
- Generated tests tightly coupled to implementation details rather than behavior
- Semantic search too narrow or too broad
- No build feedback loop (Copilot didn't verify compilation)

Only include issues that are **directly evidenced** by this run. If fewer than 10 issues are supported by the data, include only those that are.

---

## How to Extract Data from the Log

### GH Copilot Agent Mode Log
- **Timestamps:** Usually absent from individual lines; look for `Server time from HTTP Date header` entries
- **Key patterns to search for:**
  - `Begin sending message` — user messages
  - `Request content:` — prompt text
  - `SemanticSearchStrategy` — semantic search rounds
  - `FileEditingState` — file edits generated
  - `Error` / `Warning` — errors and warnings
  - `PLAN UPDATE` — plan tracking issues
  - `statusNotification` — auth/status errors
  - `MCP server` — MCP server availability
  - `EventType(9)` — LLM request calls (count for round-trip total)
  - `InputTokenCount` / `OutputTokenCount` / `TotalTokenCount` — token usage per call
  - `prompt_tokens_details_cached_tokens` — cached token count

### Copilot Diagnostic Log (from %TEMP%\VSGitHubCopilotLogs)
- **Timestamps:** Each line typically includes timestamps
- **Key patterns to search for:**
  - `request` / `response` — Copilot API interactions
  - `completion` — code completions
  - `error` / `exception` — errors

---

## Style Guidelines

- Use **bullet points** wherever possible instead of prose paragraphs
- Use **bold** for key metrics (test counts, error counts, coverage percentages, durations)
- Use **emoji** for results: ✅ success, ❌ failure, ⚠️ partial/unknown
- Use **tables** for timelines, error listings, and test classification
- Keep analysis sections concise — 3-5 bullets max per section
- Include full file paths for log files and target source files

---

## Scoring (Hybrid: Rubric + LLM Judge)
This skill computes a **rubric score (0–100)** and then applies a **bounded LLM adjustment (‑10 … +10)**.

### Rubric (0–100)
*A deterministic score built from hard signals extracted from the run—coverage deltas, compile status, test execution outcomes, and the resulting test classifications.*

Five dimensions (0–20 each):

| Dimension | 0–5 | 6–10 | 11–15 | 16–20 |
|-----------|-----|------|-------|-------|
| **Correctness** | No tests compile or run | Some tests compile but most fail | Most tests pass with minor issues | All tests pass, no compilation errors |
| **Coverage Impact** | No coverage change or decrease | <5pp coverage increase | 5–15pp coverage increase | >15pp coverage increase |
| **Behavioral Depth** | All tests trivial or redundant | <30% behavioral tests | 30–60% behavioral tests | >60% behavioral, edge cases covered |
| **Test Design Quality** | No AAA pattern, poor naming, no mocks | Inconsistent patterns, some mocks | Good AAA, proper mocks, minor gaps | Clean AAA, accurate mocks, negative cases |
| **Stability & Reliability** | Agent crashed or hung, no output | Multiple fix loops, tests deleted | Minor fix iterations, all tests kept | Clean run, no fix loops needed |

Inputs are derived from the log, coverage data, generated test files, and the Test Quality classification table.

### LLM Adjustment (‑10 … +10)
*A bounded qualitative pass that captures nuance—assertion quality, branch‑aware behavior checks, negative‑path realism, and mock‑interaction accuracy—that raw metrics miss.*

A constrained LLM reviews qualitative aspects and proposes per‑dimension deltas in **[‑2..+2]**, each justified with citations to:
- test method names,
- log timestamps (ISO 8601), or
- classification notes.

If evidence is insufficient, the delta is 0. The LLM cannot change factual metrics.

### 🏆 Final Score
Formula used: `final = clamp(rubric + Σ(deltas), 0, 100)`

Display the final score prominently with a status emoji based on the score range:
- ✅ **80–100** — Strong test generation
- ⚠️ **50–79** — Partial success, notable gaps
- ❌ **0–49** — Significant issues, low-quality output

**Format in report:**
```
## 🏆 Final Score: <score>/100 <emoji>
Rubric: <rubric_score> | LLM Adjustment: <±delta>
```

The report also includes:
- Final score, rubric score, and total LLM adjustment
- Per‑dimension breakdown
- Evidence‑linked rationale for any LLM deltas
